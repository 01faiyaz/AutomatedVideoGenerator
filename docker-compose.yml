services:
  n8n:
    image: n8nio/n8n
    ports:
      - "5678:5678"
    volumes:
      - ./n8n_data:/home/node/.n8n
      - ./media:/files/media
      - ./outputs:/files/outputs
      - ./scripts:/files/scripts
      - ./music:/files/music
    environment:
      - N8N_SECURE_COOKIE=false
